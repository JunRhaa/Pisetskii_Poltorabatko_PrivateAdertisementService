<Page x:Class="Pisetskii_Poltorabatko_PrivateAdertisementService.CompletedAdsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pisetskii_Poltorabatko_PrivateAdertisementService"
      mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
     Title="Завершенные объявления">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Завершенные объявления"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,10"
                   Foreground="#37474F"
                   Grid.Row="0"/>

        <TextBlock x:Name="TotalProfitText"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#2E7D32"
                   Margin="0,0,0,20"
                   Grid.Row="1"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="CompletedAdsList" Margin="0,5,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CCCCCC" BorderThickness="1"
                                CornerRadius="8" Margin="5" Padding="10"
                                Background="White">

                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding ImagePath}"
                                       Width="140" Height="140"
                                       Stretch="UniformToFill"
                                       Margin="5"/>

                                <StackPanel Grid.Column="1" Margin="10">
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="20"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}"
                                               TextWrapping="Wrap"
                                               Margin="0 5 0 5"/>
                                    <TextBlock Text="{Binding Price, StringFormat='Прибыль: {0} ₽'}"
                                               FontWeight="Bold"
                                               Foreground="#FF9C1A"/>
                                    <TextBlock Text="{Binding PostDate, StringFormat='Дата: {0:d}'}"
                                               Foreground="Gray"
                                               Margin="0 5 0 0"/>
                                    <TextBlock Text="{Binding CityName}" Foreground="Gray"/>
                                    <TextBlock Text="{Binding CategoryName}" Foreground="Gray"/>
                                    <TextBlock Text="{Binding AdTypeName}" Foreground="Gray"/>
                                </StackPanel>
                            </Grid>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>